apply plugin: 'com.android.application'
android {
    signingConfigs {
        config {
        }
    }
    compileSdkVersion 21
    buildToolsVersion "21.1.2"
    defaultConfig {
        applicationId "org.voidsink.anewjkuapp"
        versionCode 140033
        versionName "0.33"
        minSdkVersion 14
        targetSdkVersion 21
    }
    buildTypes {
        release {
            minifyEnabled false
            shrinkResources true
            proguardFile 'proguard-rules.txt'
            proguardFile 'proguard-rules-google-play-services.txt'
            proguardFile 'proguard-rules-ical4j.txt'
            proguardFile 'proguard-rules-keep-names.txt'
            proguardFile getDefaultProguardFile('proguard-android.txt')

            applicationVariants.all { variant ->
                appendVersionNameVersionCode(variant, defaultConfig)
            }
        }
        debug {
            minifyEnabled false
            shrinkResources true
            proguardFile 'proguard-rules.txt'
            proguardFile 'proguard-rules-google-play-services.txt'
            proguardFile 'proguard-rules-ical4j.txt'
            proguardFile 'proguard-rules-keep-names.txt'
            proguardFile getDefaultProguardFile('proguard-android.txt')
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_7
        targetCompatibility JavaVersion.VERSION_1_7
    }
    packagingOptions {
        //exclude 'META-INF/DEPENDENCIES'
        //exclude 'META-INF/NOTICE'
        //exclude 'META-INF/LICENSE'
        exclude 'META-INF/LICENSE.txt'
        exclude 'META-INF/NOTICE.txt'
        //exclude 'META-INF/ASL2.0'
    }
}

dependencies {
    compile 'com.android.support:support-v4:21.0.3'
    compile 'com.android.support:appcompat-v7:21.0.3'
    compile 'com.android.support:gridlayout-v7:21.0.3'
    compile 'com.android.support:cardview-v7:21.0.3'
    compile 'com.android.support:recyclerview-v7:21.0.3'
    compile 'com.google.android.gms:play-services-base:6.5.87'
    compile 'com.google.android.apps.dashclock:dashclock-api:2.0.0'

    compile 'org.mapsforge:mapsforge-core:0.5.0'
    compile 'org.mapsforge:mapsforge-map:0.5.0'
    compile 'org.mapsforge:mapsforge-map-android:0.5.0'

    compile 'org.jsoup:jsoup:1.8.1'
    compile 'org.mnode.ical4j:ical4j:1.0.6'

    compile 'uk.co.chrisjenx:calligraphy:2.0.2'
    compile 'com.nostra13.universalimageloader:universal-image-loader:1.9.3'
    compile 'com.github.alamkanak:android-week-view:1.2.1'
    compile 'com.timehop.stickyheadersrecyclerview:library:0.3.6'

    compile 'de.cketti.library.changelog:ckchangelog:1.2.2'
    compile 'com.mikepenz.aboutlibraries:library:4.5.8'

    compile project(':voidsinkContributors')
    compile project(':android-support-v4-preferencefragment')
    compile files('libs/mpandroidchartlibrary-2-0-7.jar')
}

def appendVersionNameVersionCode(variant, defaultConfig) {
    variant.outputs.each { output ->
        def file = output.outputFile;
        output.outputFile = new File(
                file.parent,
                file.name.replace(".apk", "-${variant.versionName}.apk"))
    }
}